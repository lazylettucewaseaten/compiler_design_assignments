void main() {
    int i;
    double result;
    bool done;
    int arr;
    arr = NewArray(10, int);

    i = 0;
    result = 0.0;
    done = false;

    while (i < 10) {
        result = result + i * 2;
        i = i + 1;
    }

    if (result >= 50) {
        printResult(result);
    } else {
        printResult(0);
    }

    for (i = 0; i < 5; i = i + 1) {
        result = result + add(i, 2);
    }

    do {
        i = i - 1;
    } while (i > 0);

    break;
    return;
}
// Sample program for syntax analyzer testing

int globalVar;
double values;
bool flag;

class MathUtils {
    int factor;
    
    int multiply(int a, int b) {
        return a * b;
    }

    void resetFactor() {
        factor = 1;
    }
}

int add(int x, int y) {
    int sum;
    sum = x + y;
    return sum;
}

void printResult(int r) {
    // imaginary print statement
}

